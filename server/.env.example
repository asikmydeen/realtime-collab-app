# Redis URL for persistence (optional - falls back to in-memory storage)
# For production, use a Redis service like Redis Cloud, Upstash, or Railway Redis
REDIS_URL=redis://localhost:6379

# Server port
PORT=3001